version: "3.1"

stories:

- story: User fills the form and starts a new session
  steps:
    - intent: reservation
      entities:
        - reservation_type: room
    - action: booking_form
    - active_loop: booking_form
    - slot_was_set:
        - requested_slot


